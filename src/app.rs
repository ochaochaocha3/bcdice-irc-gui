/// BCDice IRC GUIのアプリケーションの構造体。
pub struct Application {}

impl Application {
    /// 新しいインスタンスを返す。
    pub fn new() -> Self {
        return Self {};
    }

    /// アプリケーションを実行する。
    pub fn run(&self) {
        println!("Hello, world!");
    }
}
